# Legacy Creator Profiles

This directory uses the old `creator-profiles` structure. 
As part of the Personal Expert Network MVP, new experts should be created in the `data/experts/` directory.

Data here is kept for archival purposes and should be manually migrated to `data/experts/` as needed.
